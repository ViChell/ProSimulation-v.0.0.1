# 🚀 Швидкий старт

## Перший запуск

### 1. Встановіть залежності
```bash
cd combat_simulation
pip install -r requirements.txt
```

### 2. Перевірте систему
```bash
python test_simulation.py
```

Ви повинні побачити:
```
✓ ALL TESTS PASSED
```

### 3. Запустіть сервер
```bash
python app.py
```

### 4. Відкрийте браузер
```
http://localhost:5000
```

## 🎮 Перші кроки

1. **Step** - зробіть один крок симуляції
2. **Play** - запустіть автоматичну симуляцію
3. **Швидкість** - регулюйте швидкість (100-2000 мс)
4. **Клік на юніт** - подивіться детальну інформацію

## 📊 Що відбувається

- **Сині юніти (Side A)** рухаються на схід
- **Червоні юніти (Side B)** рухаються на захід
- Юніти автоматично атакують ворогів в радіусі дії
- Прозорість показує рівень здоров'я
- Статистика оновлюється в реальному часі

## 🔧 Зміна даних

### Змінити об'єкти
Редагуйте `data/objects.xlsx`:
- Додайте/видаліть юніти
- Змініть позиції (X_Coord, Y_Coord)
- Налаштуйте характеристики (HP, Range, Attack_Power)

### Змінити правила
Редагуйте `data/sets.xlsx`:
- Аркуш "Engagement_Rules" - ймовірності ураження
- Аркуш "Combat_Modifiers" - модифікатори бою

### Застосувати зміни
1. Збережіть Excel файли
2. Натисніть **Reset** в інтерфейсі
3. Або перезапустіть сервер

## 🎯 Приклади модифікації

### Додати більше танків Side A
1. Відкрийте `data/objects.xlsx`
2. Скопіюйте рядок з танком Side A
3. Змініть ID на новий
4. Змініть координати
5. Збережіть файл

### Змінити дальність артилерії
1. Відкрийте `data/objects.xlsx`
2. Знайдіть артилерійські юніти
3. Змініть значення в колонці "Range"
4. Збережіть файл

### Зробити танки сильнішими
1. Відкрийте `data/sets.xlsx`
2. Знайдіть рядки з "tank" в Attacker_Type
3. Збільште Damage_Multiplier
4. Збережіть файл

## ❓ Проблеми?

### Помилка "Module not found"
```bash
pip install -r requirements.txt --break-system-packages
```

### Карта не завантажується
- Це нормально, якщо у вас немає MBTiles файлу
- Система використовує OpenStreetMap за замовчуванням
- Для використання власних карт:
  1. Отримайте .mbtiles файл
  2. Оновіть MBTILES_PATH в config.py

### Симуляція не запускається
```bash
python test_simulation.py
```
Це покаже де проблема

## 📚 Далі

Читайте [README.md](README.md) для детальної документації:
- Архітектура системи
- API endpoints
- Розширення функціональності
- Додавання нових типів юнітів

## 💡 Поради

1. **Починайте просто** - спершу змініть кілька параметрів
2. **Тестуйте часто** - перевіряйте після кожної зміни
3. **Дивіться логи** - консоль показує що відбувається
4. **Експериментуйте** - система дозволяє швидко пробувати ідеї

## 🎖️ Успіхів!

Якщо все працює - ви готові моделювати бойові операції! 🚀
